# PLAN.md — план проекта «ВК-бот Розыгрыш постеров»

Статус: **Фазы 0–1 завершены.** Итоговое ТЗ — `ТЗ_ИТОГ.md`, архитектура —
`ARCHITECTURE.md`. Дальше — реализация (Фаза 2) по этапам. Чекбоксы отмечает оркестратор.

---

## Фаза 0 — Уточнение ТЗ ✅ ЗАВЕРШЕНА

- [x] Способ проверки оплаты → OCR + тумблер автоподтверждения + ручная очередь.
- [x] Стек → Python + FastAPI + PostgreSQL.
- [x] OCR-движок → Tesseract локально.
- [x] Кол-во постеров → сумма ÷ цена.
- [x] Источник данных → Postgres, Google Sheets — авто-создаваемое зеркало.
- [x] Инфраструктура → ВК-токен есть, Google-аккаунт есть, админка по IP.
- [x] Сформировать итоговое ТЗ → `ТЗ_ИТОГ.md`.
- [ ] Отправить заказчику открытые вопросы (`ТЗ_ИТОГ.md §16`) — параллельно.

## Фаза 1 — Архитектура и стек ✅ ЗАВЕРШЕНА

- [x] Выбрать стек и хостинг (см. Журнал решений).
- [x] Финализировать модель данных (`ТЗ_ИТОГ.md §6`): таблицы, индексы, статусы,
      ON DELETE CASCADE на `event_id`.
- [x] Спроектировать потоки/интеграции: воркер-polling (5 сек), безопасные
      плейсхолдеры, выбор номеров через `generate_series ... ORDER BY random()`,
      OCR-pipeline, синк Sheets — всё в `ARCHITECTURE.md`.
- [x] Структура репозитория (`ARCHITECTURE.md §3`).
- [x] Заполнить проектные секции `CLAUDE.md`.

## Фаза 2 — Реализация ⟵ ТЕКУЩАЯ

Порядок этапов — снизу вверх по зависимостям. У каждого — критерий приёмки (КП).

- [x] **2.1 Каркас.** Структура из `ARCHITECTURE.md §3`, `requirements.txt`,
      `.env.example`, `config.py`, `db.py`, инициализация Alembic (async env.py).
      КП: импорты `app.core.config/db` ок, alembic-конфиг грузится, env.py с
      `Base.metadata` и URL из settings. Полный `upgrade head` требует живую БД.
- [x] **2.2 Модели + миграция.** `app/core/models.py` — 5 таблиц (Event,
      Participant per-event, Purchase, PosterNumber, BotDialogState), enum
      `PurchaseStatus`, каскады, unique `(event_id,number)`/`(event_id,vk_user_id)`,
      частичный индекс активного keyword. Миграция `0001_initial_schema` (вручную,
      т.к. PG нет). КП: схема валидна на SQLite (тест `tests/test_models_schema.py`),
      uniq `(event_id, number)` подтверждён. `upgrade head` к PG не гонялся (нет БД).
- [x] **2.3 Сервисы core (без бота/веба).** `placeholders` (безопасный render),
      `defaults` (DEFAULT_TEXTS), сервисы `events`/`numbers`/`purchases`/
      `participants`/`winners`. assign_unique — Python random.sample. КП выполнен:
      25 тестов зелёные (`tests/test_services.py`) — уникальность/исчерпание,
      расчёт posters_count, revoke/free, рендер, decide_after_ocr, каскадное удаление.
- [x] **2.4 OCR-модуль.** `ocr/recognize.py` (Pillow-препроцессинг + pytesseract
      rus+eng через to_thread; путь к бинарю из settings; не падает без бинаря,
      `tesseract_available()`), `ocr/parse.py` (parse_amount по ключевым словам/валюте,
      find_recipient со срезкой форм собственности, parse_receipt). КП: 16 юнитов
      parse.py зелёные (41 passed, 1 skipped — реальный OCR без бинаря Tesseract).
- [x] **2.5 Sheets.** `sheets/client.py` (ленивая gspread-авторизация),
      `sheets/sync.py`: build_rows, create_sheet (3 колонки + share reader),
      rebuild/add_rows/remove_rows, collect_event_records (только approved),
      rebuild_event_sheet из БД. Все сетевые вызовы в to_thread+try/except. КП:
      6 тестов на моках (формат строк, share reader, проглатывание ошибок API,
      approved-фильтр) — 47 passed, 1 skip.
- [x] **2.6 Бот — диалог.** `bot/dialog.py` (чистая логика без vkbottle:
      is_keyword_match по границам слова, is_event_open, find_matching_event,
      get/set/clear dialog, receipt_hash, анти-дубль, process_receipt), `handlers.py`
      (vkbottle-обвязка: кодовое слово→msg_instruction+QR, молчание вне сценария,
      приём чека+имя+телефон→msg_receipt_received), `main.py` (Long Poll). КП:
      24 юнита dialog.py (71 passed, 1 skip). Ручной прогон сценария — у заказчика.
- [x] **2.7 Бот — воркер.** `bot/worker.py`: ядро `process_pending(session, *,
      send_message, sync_sheet)` (DI-колбэки → тестируемо) — approved & !assigned →
      assign_unique → numbers_assigned=True (до отправки, идемпотентно) →
      `msg_after_payment` (+`msg_need_contacts` при пустых контактах) → add_rows в
      Sheets. NumbersExhausted → лог-алерт, не падает. `worker_loop` + запуск в
      main.py рядом с Long Poll. КП: 8 юнитов (79 passed, 1 skip).
- [x] **2.8 Админка — auth.** `admin/auth.py` (bcrypt-сверка с `.env`, /login,
      /logout), `admin/deps.py` (require_login→редирект, get_session), `admin/main.py`
      (FastAPI + SessionMiddleware + статика + Jinja2), шаблоны base/login/index + CSS,
      `scripts/gen_password_hash.py`. Пин `bcrypt<4.1` (passlib 1.7.4 несовместим).
      КП: 6 httpx-тестов (85 passed, 1 skip) — вход/отказ/защита роутов/logout.
- [x] **2.9 Админка — мероприятия.** `admin/routes/events.py` — список, создание
      (все настройки + дефолтные тексты + QR-загрузка + авто-создание Google-листа),
      редактирование, toggle (стоп/старт), удаление (каскад БД + delete_sheet + файлы
      чеков/QR). Шаблоны events_list/event_form. КП: 6 httpx-тестов (91 passed, 1 skip)
      — создание/список/правка/toggle/каскад-удаление/изоляция других событий.
- [x] **2.10 Админка — модерация.** `admin/routes/moderation.py` — очередь с
      фильтром по статусу + поиск (имя/vk_name/vk_id/телефон/номер/сумма) + пометка
      частичной оплаты; карточка с полной инфо; чек через FileResponse (404-гард);
      approve↔reject (moderated_by); revoke (освобождение номеров + rebuild_event_sheet);
      set_amount (пересчёт); правка имени/телефона. Шаблоны moderation_list/
      purchase_detail. КП: 9 httpx-тестов (100 passed, 1 skip).
- [x] **2.11 Админка — участники + победители.** `admin/routes/participants.py`
      (список участников события с присвоенными номерами, селектор события),
      `admin/routes/winners.py` (генератор `pick_winners` — N неповторяющихся из
      оплаченных, display-only без персиста — таблицы winners в схеме нет). Шаблоны
      participants_list/winners. КП: 5 httpx-тестов (105 passed, 1 skip) — номера в
      списке, ровно winners_count (или меньше при нехватке) только из присвоенных.
- [x] **2.12 Деплой.** systemd (2 сервиса), Caddy-фрагмент (по IP, порт 8080,
      `import` в существующий Caddy), README (ручной деплой). КП: разворачивается
      на Ubuntu, бот и админка работают. (Было nginx+deploy.sh — заменено на Caddy
      и ручную копипаст-инструкцию по требованию заказчика, см. Журнал.)
- [x] **2.13 Приёмка** по критериям `ТЗ_ИТОГ.md §15` (9 пунктов). Все 9 покрыты
      кодом и тестами (105 passed, 1 skip — реальный OCR без бинаря). Пункты 1–8 —
      проверены автотестами и ревью; п.9 (развёртывание на сервере) — конфиги/скрипты
      готовы (systemd×2, Caddy-фрагмент, README), фактический деплой — на боевом
      сервере заказчика (нужны VK-токен, SA-json, IP). **Фаза 2 ЗАВЕРШЕНА.**

---

## Фаза 3 — UX-конфигурация для заказчика ✅ ЗАВЕРШЕНА

Цель: заказчик настраивает интеграции из админки, без правки файлов/«кишок».

- [x] **3.1** Таблица `app_settings` (key/value) + модель `AppSetting` + миграция
      `0002_app_settings`; сервис `core/services/app_settings.py`.
- [x] **3.2** Шифрование секретов: `core/crypto.py` (Fernet), `SECRETS_KEY` в `.env`,
      `scripts/gen_secrets_key.py`. Шифруется `vk_token`; в HTML не отдаётся.
- [x] **3.3** Бот грузит VK-токен из БД с fallback на `.env` (`bot/main.py`).
- [x] **3.4** `create_sheet(owner_email)` авто-шарит лист владельцу как «редактор»;
      `events.py` берёт почту из настроек.
- [x] **3.5** Админка `/settings`: ввод токена/group_id/почты + кнопки «Проверить VK»
      (`bot/vk_check.py`) и «Проверить Google Sheets» (`sheets/sync.test_connection`).
- [x] **3.6** Тесты (`tests/test_settings.py`): crypto, сервис, роуты, vk_check.
      **118 passed, 1 skipped.** **Фаза 3 ЗАВЕРШЕНА.**

---

## Фаза 5 — Фидбек заказчика после демо ⟵ ТЕКУЩАЯ

Триггер: обширный фидбек заказчика (`edit.md`) — UI некрасивый/непонятный,
нет Google Sheets, нет темной темы, формы кривые.

**Порядок этапов — по зависимостям.** Каждый этап: оркестратор проектирует,
сабагент пишет код, потом деплой + проверка.

### 5.0 Тёмная тема + базовый редизайн шаблонов
- [x] **5.0.1 CSS-тёмная тема.** Добавить `dark` CSS-переменные в `style.css`
      (data-атрибут `data-theme="dark"` на `<html>`). Кнопка переключения в шапке
      (localStorage). Затронуть ВСЕ компоненты: карточки, таблицы, формы, бейджи,
      инпуты, переключатели.
- [x] **5.0.2 Обновить base.html.** Переключатель темы в шапке (иконка луна/солнце).
      Название главной — из настроек (app_settings, ключ `admin_title`,
      дефолт `Админка`).
- [x] **5.0.3 Редизайн всех шаблонов.** Компактнее, аккуратнее: убрать
      «растянутость», улучшить типографику, отступы, выравнивание.

### 5.1 Форма мероприятия — доработка
- [x] **5.1.1 Звёздочки обязательных полей.** `<span class="req">*</span>` к
      обязательным полям.
- [x] **5.1.2 Предпросмотр QR.** FileReader в JS, превью при загрузке + текущий QR.
- [x] **5.1.3 Юридические лица.** Подсказка в `expected_recipient` для ООО/ИП/ФИО.
- [x] **5.1.4 Google-таблица (опция).** Поле `google_sheet_url` в форме + колонка
      в модели. Миграция `0004`. Бэкенд хендлеры обновлены.
- [x] **5.1.5 Единый блок сообщений.** Объединены «Какие сообщения» и «Тексты»
      в один блок: тумблер + textarea + чипы. Единый набор переменных для всех полей.

### 5.2 Список мероприятий — редизайн
- [x] **5.2.1 Компактная таблица.** Цена/номера — `white-space:nowrap`. Даты —
      компактные. Убраны лишние колонки. Кнопки компактные.

### 5.3 Модерация — VK-ссылка на чат
- [x] **5.3.1 Ссылка на чат.** В списке и в деталях покупки — ссылка
      `https://vk.com/write-{group_id}&vk_user_id={vk_user_id}` для чата
      от лица сообщества.

### 5.4 Настройки — доработка
- [x] **5.4.1 Название админки.** Поле `admin_title` в `/settings`, дефолт `Админка`.
- [x] **5.4.2 Отключение вкладки «Победители».** Переключатель `winners_tab_enabled`
      в настройках. Если выключен — ссылка скрыта в навигации и на дашборде.

### 5.5 Google Sheets — второй режим
- [x] **5.5.1 Модель.** Колонка `Event.google_sheet_url` (Text, nullable).
      Миграция `0004`.
- [x] **5.5.2 Sync-логика.** В `sheets/sync.py` — функция
      `sync_event_to_sheet(event, google_sheet_url)`. Если таблица пустая —
      создаёт шапку (Номер | Имя | Оплачено). Если заполнена — проверяет
      формат и корректирует. Синхронизирует approved-номера.
- [x] **5.5.3 Воркер + бот.** Если `event.google_sheet_url` задана —
      `{sheet_url}` = ссылка на Google-таблицу (без права редактирования
      для читателей). Если нет — `/p/{event_id}`.
- [x] **5.5.4 Админка.** В форме мероприятия — поле для ссылки на Google
      таблицу.

### 5.6 Тесты
- [x] **5.6.1** Обновить существующие тесты под новые поля (google_sheet_url,
      admin_title, show_winners_tab). Добавить тесты на OCR-парсер для
      юридических лиц. 123+ passed.
- [x] **5.6.2** Тесты на тёмную тему (проверка data-атрибута, localStorage).

### 5.7 Деплой
- [x] **5.7.1** `alembic upgrade head` (миграции 0003+0004), pytest, деплой
      на боевой сервер, smoke-проверка.

---

## Фаза 6 — Доводка фидбека + полный редизайн фронтенда ⟵ ТЕКУЩАЯ

Триггер: повторная ревизия `edit.md` показала, что часть пунктов реализована
неверно/частично, и заказчик требует ПОЛНОЙ переделки фронтенда «с нуля» с
обязательным использованием скилов `frontend-design-direction` и
`design-taste-frontend` (подключены при проектировании этой фазы).

**Дизайн-направление (зафиксировано скилами):** внутренний ops-инструмент,
плотный/спокойный/сканируемый, НЕ лендинг. Диалы: VARIANCE 3, MOTION 2,
DENSITY 6. Стек остаётся ванильным (Jinja2 + CSS/JS). Locks: единый акцент
(indigo), одна шкала радиусов, одна шкала отступов, контраст WCAG AA, тёмная
тема без чистого #000/#fff, статус-точки только семантические,
`prefers-reduced-motion` уважать.

### 6.1 Фиксы поведения (backend-баги из ревизии edit.md)

- [x] **6.1.1 VK-ссылка на чат — ИСПРАВЛЕНО.** Сейчас в `moderation_list.html:63`
      и `purchase_detail.html:45` ссылка `vk.com/write-{event_id}&vk_user_id=…`
      — БАГ: подставляется `event_id` (id мероприятия в БД) вместо VK group_id,
      и формат URL невалиден (`&` без `?`, `write-` так не работает).
      **Как чинить:** в `admin/routes/moderation.py` (роуты `moderation_list` и
      `purchase_detail`) читать `vk_group_id = await app_settings.get_setting(
      session, KEY_VK_GROUP_ID)` и прокидывать в контекст шаблона. Ссылку строить
      `https://vk.com/gim{group_id}?sel={vk_user_id}` (открывает сообщения
      сообщества с выбранным юзером — «от лица сообщества»). Если group_id не
      задан — ссылку НЕ показывать. Обновить оба шаблона.
      КП: в HTML ссылка вида `gim{group_id}?sel={uid}`; при пустом group_id ссылки нет.
- [x] **6.1.2 `{sheet_url}` по режиму — ИСПРАВЛЕНО.** Сейчас `worker.py:77` и
      `handlers.py:60` ВСЕГДА дают `/p/{event_id}`, даже когда задан
      `google_sheet_url` (противоречит edit.md: в режиме Google Sheets участник
      должен получать ссылку на гугл-таблицу без права редактирования).
      **Как чинить:** в `sheets/sync.py` добавить `def reader_url(url: str) -> str`
      — извлечь id (`extract_sheet_id`) и вернуть read-only вид
      `https://docs.google.com/spreadsheets/d/{id}/preview`. В `worker.py` (ctx) и
      `handlers.py:_build_ctx` сделать: `sheet_url = sync.reader_url(
      event.google_sheet_url) if event.google_sheet_url else
      public_table.public_table_url(event.id)`. Импорт `reader_url` лениво (как
      `sync_event_to_sheet`), чтобы не тянуть gspread в бот-логику.
      КП: при заданном `google_sheet_url` участнику уходит `/preview`-ссылка;
      без неё — `/p/{event_id}`.
- [x] **6.1.3 Сообщение «просьба контактов» — УБРАНО из UX.** edit.md (стр. 12):
      «сообщение с просьбой прислать имя и номер — уходит» (имя+телефон теперь
      приходят вместе с чеком). **Как чинить:** убрать 4-й блок (msg_need_contacts)
      из `event_form.html`; в `services/events.py`/дефолтах ставить
      `send_need_contacts=False` по умолчанию; в `worker.py` блок отправки
      `msg_need_contacts` оставить в коде, но он не сработает при выключенном флаге
      (страховка). Колонку в БД не удаляем (обратная совместимость).
      КП: новой формы без блока «Просьба контактов»; новое мероприятие не шлёт это
      сообщение.

### 6.2 Документация решений

- [x] **6.2.1** В «Журнал решений» зафиксировать: (а) «второй режим» реализован как
      поле `google_sheet_url` на мероприятие (не отдельная админка) — упрощение,
      заказчик тестирует пер-ивент; (б) права доступа к гугл-таблице: админ даёт
      ОДНУ ссылку с правом редактирования (под service account), читателям бот шлёт
      производную `/preview`; таблица должна быть расшарена «по ссылке: просмотр» —
      это зона админа (написать подсказку в форме у поля google_sheet_url).

### 6.3 Редизайн фронтенда (design-system, по скилам)

> **РЕШЕНИЕ ЗАКАЗЧИКА 2026-06-26 (override):** заказчик посмотрел текущий фронт
> (токен-система Фазы 5) и забраковал его как «кривой» / некачественный по вёрстке.
> **Делаем ПОЛНЫЙ РЕВРАЙТ фронта с нуля** (не полировка). Скилы
> frontend-design-direction + design-taste-frontend. Сначала — реальный осмотр
> текущего фронта через agent-browser (диагностика косяков вёрстки), затем
> чистая пересборка `style.css` + всех 10 шаблонов, проверка обеих тем и мобилы
> в реальном браузере. Дизайн-направление: ops-инструмент, плотный/спокойный/
> сканируемый, выровненный по сетке; единый акцент indigo; обе темы.

- [x] **6.3.1 Токен-слой `style.css`.** Реврайт с нуля (обе темы, Семантические CSS-переменные для светлой и
      тёмной тем: `--bg, --surface, --surface-2, --border, --text, --text-muted,
      --accent (indigo), --accent-contrast, --danger, --success, --warning`. Off-black
      (не #000) / off-white. Одна шкала отступов (4/8/12/16/24/32), одна шкала
      радиусов, focus-ring видимый, переходы только `transform/opacity` под
      `@media (prefers-reduced-motion: no-preference)`. Шрифт Manrope оставить.
      КП: переключение темы меняет ВСЕ компоненты, контраст AA в обеих темах.
- [x] **6.3.2 Шапка + `base.html`.** Нав в одну строку (≤72px), бургер на мобиле,
      переключатель темы, активный пункт, имя пользователя. Название из настроек.
- [x] **6.3.3 Компоненты.** Единый набор: кнопки (контраст AA, `:active`-нажатие),
      бейджи/статусы, карточки (только где нужна иерархия), таблицы (без
      `white-space:nowrap`-костылей — нормальное выравнивание `.num`, усечение
      длинных значений), формы (label сверху, hint снизу, switch-тумблеры,
      звёздочки `.req`), filter-pills, banners, empty-state. Никаких карточек
      внутри карточек.
- [x] **6.3.4 Перевёрстка страниц** под компактность/сканируемость:
      `events_list`, `event_form`, `moderation_list`, `purchase_detail`,
      `participants_list`, `winners`, `settings`, `public_table`, `index`, `login`.
      Каждая — плотная, аккуратная, выровненная. Проверить в обеих темах и на мобиле.

### 6.4 Тесты + деплой

- [x] **6.4.1** Тесты: (а) chat-link содержит `gim{group_id}?sel=` и не содержит
      `write-`; (б) `reader_url` отдаёт `/preview`; ctx участника = preview при
      заданном `google_sheet_url`, иначе `/p/{id}`; (в) новое мероприятие
      `send_need_contacts is False`; (г) обновить снапшоты темы/шаблонов под новые
      классы. Все зелёные.
- [x] **6.4.2** `pytest` → деплой на боевой сервер → smoke (логин, все страницы 200,
      переключение темы, обе темы визуально). ВЫПОЛНЕНО: push de05025 → GitHub,
      `git reset --hard origin/main` на /opt и /root (по согласованию заказчика —
      на сервере были незакоммиченные SFTP-правки Phase 5, уже вошедшие в коммит),
      alembic на head (0004), deps на месте, сервисы active, smoke :8080 /login=200,
      редиректы 303 корректны, новый фронт отрисовывается на боевом (agent-browser).

---

## Фаза 7 — Фидбек заказчика: QR + PDF-чеки + мелочи ⟵ ТЕКУЩАЯ

- [x] **7.1.1** Вернуть тумблер `send_qr` в `event_form.html` (выпал при реврайте
      Фазы 6 → новые мероприятия сохранялись с send_qr=False, бот не слал QR).
      Default checked на создании.
- [x] **7.1.2** Починить превью QR: добавлен auth-роут `GET /events/{id}/qr`
      (FileResponse), превью переключено с несуществующего `/static/qr/` на него.
- [x] **7.1.3** Регресс-тесты (форма содержит send_qr; send_qr=on сохраняется).
      Тесты 161 passed, 1 skipped.
- [ ] **7.1.4** Деплой: push пользователя → git pull /opt+/root → restart →
      smoke → `UPDATE events SET send_qr=true WHERE id=4` → проверка QR ботом.
- [ ] **7.2** PDF-чеки: OCR читает чеки в PDF (рендер 1-й стр. через pypdfium2/
      pdf2image перед Tesseract) + превью PDF в `purchase_detail.html`. Деплой.
- [ ] **7.3** (не срочно) Создание мероприятия с дубль-кодовым словом → понятная
      ошибка в форме вместо 500 (нарушение unique-индекса keyword).
- [ ] **7.4** (не срочно) Апдейт имени/телефона участника без новой покупки должен
      обновлять строку публичной таблицы (сейчас имя обновляется только при новой
      подтверждённой покупке).

---

## Фаза 8 — Большой фидбек заказчика (edit.md, ревизия 2026-06-26) ⟵ ТЕКУЩАЯ

Источник — `edit.md` (13 пунктов). Уточнения заказчика (AskUserQuestion 2026-06-26):
- **Публичное имя = всегда имя из ВК** (first_name); полное ФИО от пользователя
  хранится для карточки/модерации; админ может переопределить публичное имя вручную.
- **Авто-подтверждение:** сумма ≥ цены билета И найден получатель → approved;
  кол-во билетов = floor(сумма/цена), остаток игнор (НЕ требовать точной кратности).
- **Abuse:** глобальный дедуп (хэш файла + реквизиты across мероприятий/юзеров) +
  обязательный получатель + проверка свежести даты чека; подозрительное → ручная
  очередь с красным флагом.
- **«Ручное добавление брони» = кнопка «Добавить вручную» в модерации.** Создаёт
  чек+псевдо-участника, поля: ссылка ВК, ФИО, телефон, файл чека (загружает админ),
  сумма, публичное имя; кнопки одобрить/отклонить; при одобрении присваиваются номера.
  Назначение: подстраховка, когда бот затупил, а оплата реально прошла.

**Приоритизация (сверху — первым).** Логика: сначала чиним сломанный платёжный
конвейер (без него продукт не работает), потом диалоговый флоу и данные, потом
UX модерации, потом тяжёлые UI-переделки.

**РАБОЧЕЕ СОГЛАШЕНИЕ (заказчик, 2026-06-26):** **1 этап = 1 сессия.** После каждого
этапа заказчик стартует НОВУЮ сессию с чистым контекстом и продолжает по плану.
Поэтому в конце КАЖДОГО этапа ОБЯЗАТЕЛЬНО: всё зафиксировать в STATE.md и PLAN.md
(что сделано, результат, следующий этап, грабли), чтобы следующая сессия продолжила
без потери контекста. Деплой — **инкрементальный по группам** (push → git pull
/opt+/root → restart → smoke). Токен ВК **имеет** право «Фотографии» (заказчик
подтвердил) — версия про отсутствие права для #2 ОТПАДАЕТ.

**Сквозное правило фазы:** после КАЖДОЙ группы — `pytest` зелёный + **end-to-end
проверка через скил `agent-browser`** (локальная SQLite-QA: `scripts/local_dev_seed.py`
+ uvicorn :8000) + деплой группы на боевой. Бот-флоу, где живой ВК недоступен,
проверяем юнит/интеграционными тестами логики + ручным прогоном заказчиком.

### 8.0 Баг #2: картинка/QR не всегда отправляется + перестать глотать ошибки
Фаза 7.1 (тумблер send_qr) УЖЕ задеплоена, но баг #2 ЖИВ. Токен право «Фото» имеет →
причина не в токене. Создание/правка используют один `_save_qr_file`, форма с
`enctype=multipart/form-data`, `create_event` сохраняет qr_image_path — т.е. не в
форме/персистенции на первый взгляд. «Переключение галочки починило» — надо понять,
почему. Диагностика — по логам vk-bot и состоянию events в БД на боевом.
- [ ] **8.0.1 Диагностика на сервере.** SSH (ssh.md: root@185.228.72.118), logs
      `journalctl -u vk-bot`: искать исключение при `PhotoMessageUploader.upload`
      (какое именно), `SELECT id,send_qr,qr_image_path FROM events`. Проверить, что
      файл qr_image_path реально существует на диске. Вывод: токен/право, путь None,
      путь не существует, или иная ошибка upload.
- [ ] **8.0.2 Фикс корня** по результату диагностики (вероятные кандидаты: на первом
      создании qr_image_path не сохранился/файл не на диске; либо upload падает по
      конкретной причине, которую видно только в логах).
- [ ] **8.0.3 Не глотать ошибки (требование заказчика).** Ошибку отправки QR/фото
      больше НЕ прятать молча. Сделать видимой админу: писать понятный лог + сохранять
      «последняя ошибка отправки QR» на мероприятии (колонка `last_qr_error` или
      app_setting) и показывать предупреждение в форме/списке мероприятий, либо иной
      механизм уведомления админа. Инструкция всё равно должна доходить (best-effort
      сохраняем), но факт несработавшего QR — заметен.
- [ ] **8.0.4** Тесты + e2e (agent-browser админка) + деплой + фиксация в STATE/PLAN.
      **Скилы:** test-driven-development, fastapi-patterns, agent-browser. **Агент:**
      диагностика+проектирование — оркестратор; код фикса — coder (sonnet).

### P1 — Платёжный конвейер (ядро, сейчас сломано)

#### 8.1 Дубль кодового слова → дружелюбная ошибка (edit.md #1)
- [ ] **8.1.1** В `events.py` (create/update) ловить нарушение unique-индекса
      `uq_event_keyword_active` (keyword среди активных). ДО вставки — `SELECT` события
      с таким keyword среди активных; если есть — рендерить форму повторно с ошибкой
      «Кодовое слово уже используется в мероприятии "<name>" (#id)». На уровне БД —
      try/except IntegrityError как страховка (rollback + та же ошибка). КП: создание
      дубль-keyword показывает сообщение с именем конфликтующего мероприятия, не 500.
      **Скилы:** fastapi-patterns, test-driven-development. **Агент:** coder (sonnet).

#### 8.2 PDF-чеки: OCR + превью (edit.md #8)
- [ ] **8.2.1** OCR PDF. Либа **pypdfium2** (pip, без системных зависимостей, кроссплат
      Win/Linux). В `ocr/recognize.py`: если файл .pdf → рендер 1-й страницы в PIL.Image
      (pypdfium2, scale ~2-3) → дальше существующий пайплайн Tesseract. Ветка по
      расширению/сигнатуре `%PDF`. В `handlers._extract_receipt_attachment` PDF-doc уже
      подхватывается (doc.ext). Добавить pypdfium2 в requirements.
- [ ] **8.2.2** Превью PDF в `purchase_detail.html`: если receipt — pdf, отдавать
      `<embed>/<iframe>` на роут receipt (Content-Type application/pdf) или ссылку
      «Открыть PDF». Роут `moderation_receipt` отдать с корректным media_type по
      расширению. КП: PDF-чек распознаётся (сумма/получатель), превью открывается.
      **Скилы:** fastapi-patterns, test-driven-development; agent-browser (превью).
      **Агент:** coder (sonnet).

#### 8.3 Авто-подтверждение + abuse (edit.md #3, #12)
- [ ] **8.3.1 Фикс evaluate_payment.** В `purchases.py`: убрать требование точной
      кратности (`amount % price == 0`). Условие approved при auto_confirm:
      `amount >= price AND recipient_found AND not abuse`. posters_count = floor.
- [ ] **8.3.2 Дата чека.** В `ocr/parse.py` добавить `parse_receipt_date(text)`
      (регэксп дат `dd.mm.yyyy`, `dd month yyyy` рус., ISO). Колонка
      `Purchase.receipt_date` (Date, nullable) + миграция. Заполнять при приёме чека.
- [ ] **8.3.3 Дедуп + abuse-гейт.** Сервис `core/services/abuse.py`:
      `is_duplicate_global(session, receipt_hash, exclude_purchase_id=None)` — есть ли
      approved/любой Purchase с тем же hash в ЛЮБОМ мероприятии. `signature` дедуп по
      (amount+date+recipient_norm). `is_date_fresh(receipt_date, now, max_age_days)` —
      дата не в будущем и не старше N дней (N в .env/настройке, дефолт 14).
      decide_after_ocr: если auto_confirm И платёж валиден, НО (дубль глобально ИЛИ
      дата несвежая) → manual_review с флагом `needs_attention` (см. 8.9 красный флаг).
      Дубль больше НЕ просто помечается — блокирует авто-апрув. КП: повторный чек
      и чек с будущей/старой датой не авто-подтверждаются; «чистый» — авто-подтверждается.
      **Скилы:** test-driven-development, fastapi-patterns. **Агент:** coder (sonnet);
      ревью гейта — architect (opus) если логика дедупа окажется нетривиальной.

### P2 — Диалоговый флоу + данные участника

#### 8.4 Новый сценарий бота: запрос ФИО+телефон после чека (edit.md #4)
Флоу: keyword → msg_instruction(+QR) → пользователь шлёт ЧЕК → msg_receipt_received
(«чек принят, ожидайте проверки, пришлите ФИО и телефон») → пользователь шлёт ФИО+тел
→ **новое** msg_contacts_saved («данные приняты») → (после модерации) msg_after_payment
(с номерами). Всё под тумблерами (вкл/выкл/редактирование текста), как остальные.
- [ ] **8.4.1 Стадии диалога.** В `BotDialogState` колонка `stage` (Text:
      `awaiting_receipt` | `awaiting_contacts` | `done`), миграция. set_dialog ставит
      `awaiting_receipt`. После приёма чека → `awaiting_contacts`. После приёма
      контактов → `done`.
- [ ] **8.4.2 Роутинг сообщений в handlers.** Текущее сообщение интерпретировать по
      стадии: (а) есть вложение-чек → ветка чека (как сейчас) → стадия awaiting_contacts;
      (б) стадия awaiting_contacts И текст без вложения И похоже на контакты (есть
      телефон ИЛИ ≥2 слов) → распарсить ФИО(provided_name=full)+phone, обновить
      участника, msg_contacts_saved; (в) текст = другое кодовое слово → переключить
      диалог (find_matching_event) даже из awaiting_contacts; (г) иначе (мусор в
      awaiting_contacts) → НЕ терять стадию, мягко повторить просьбу (msg короткий
      повтор / молчать — НЕ ломаться). Бот не должен «тупить»/терять контекст и не
      зависеть от того, пришли ли контакты ДО или ПОСЛЕ модерации.
- [ ] **8.4.3 ФИО vs публичное имя.** `provided_name` = полное ФИО (как прислал юзер).
      Новая колонка `Participant.public_name` (Text, nullable, админ-override). Новая
      колонка `Participant.vk_first_name` (Text) — заполняется из VK API (users.get
      first_name). Резолвер публичного имени: `public_name or vk_first_name or
      <первый токен provided_name> or vk_name`. Обновить `public_table.collect_records`
      и worker-ctx (`name`) на резолвер. Миграция на обе колонки.
- [ ] **8.4.4 Тумблер + дефолтный текст.** Поле/тумблер msg_contacts_saved в форме
      мероприятия и defaults.py; msg_receipt_received переформулировать («…пришлите
      ФИО и телефон»). msg_need_contacts (старое) оставить выключенным/легаси.
      КП: юнит-тесты FSM (все ветки a–г); публичное имя = VK first_name; полное ФИО в
      карточке/модерации. **Скилы:** test-driven-development. **Агент:** coder (sonnet);
      проектирование FSM — оркестратор (готовая таблица переходов в ТЗ агенту).

### P3 — UX модерации (зависит от ФИО-данных и флоу)

#### 8.5 Объединить «Отклонить»/«Отозвать» в одну кнопку (edit.md #5)
- [ ] **8.5.1** Оставить только «Отклонить». Семантика: освобождает забронированные
      номера (free_numbers) + статус rejected + numbers_assigned=False. Т.е. reject
      теперь делает то, что раньше revoke (но статус rejected, не revoked). Убрать
      кнопку/роут revoke из UI (роут оставить как алиас для обратной совместимости либо
      удалить и поправить тесты). Публичная таблица (approved-only) сразу перестаёт
      показывать номера. КП: «Отклонить» по approved-покупке освобождает номера и
      убирает из таблицы. **Скилы:** test-driven-development. **Агент:** coder (sonnet).

#### 8.6 OCR-сумма автоподстановка в поле правки (edit.md #6)
- [ ] **8.6.1** В `purchase_detail.html` поле «сумма» (форма set_amount)
      `value = purchase.amount or purchase.ocr_amount or ''`. КП: при ocr_amount поле
      предзаполнено. **Скилы:** —. **Агент:** coder-simple (haiku).

#### 8.7 Автоподстановка имени/телефона в модерации + публичное имя (edit.md #7)
- [ ] **8.7.1** Поля контактов в `purchase_detail.html` предзаполнять из участника:
      телефон = participant.phone; «публичное имя» (новое отдельное поле, пишет в
      `public_name`) = резолвер (public_name or vk_first_name); показывать рядом полное
      ФИО (provided_name, read-only/инфо). Форма contacts: phone + public_name (+ при
      желании правка provided_name). Подчеркнуть в UI: публичное имя уходит в публичную
      таблицу — только имя, не ФИО. КП: поля предзаполнены; правка public_name меняет
      публичную таблицу. **Скилы:** fastapi-patterns. **Агент:** coder (sonnet).

#### 8.8 Кнопка «Добавить вручную» (edit.md #13 / уточнение)
- [ ] **8.8.1** Роут+форма `/moderation/manual` (GET форма, POST создание). Поля:
      vk_link (ссылка/или id), ФИО (provided_name), phone, public_name, amount,
      event_id (выбор мероприятия), файл чека (UploadFile, сохранить в receipts_dir).
      Логика: распарсить vk_user_id из ссылки если можно, иначе синтетический
      отрицательный id (уникальность (event_id, vk_user_id)); upsert участника;
      создать Purchase(status=manual_review, amount, receipt_file_path, posters_count=
      floor). Дальше обычные кнопки одобрить/отклонить (одобрение → воркер присвоит
      номера). КП: ручной чек создаётся, одобряется, номера присваиваются, попадает в
      таблицу. **Скилы:** fastapi-patterns, test-driven-development, agent-browser.
      **Агент:** coder (sonnet).

#### 8.9 Модерация-канбан: 3 колонки + фильтр + «Списком» + флаг (edit.md #11, #19)
- [ ] **8.9.1 Бэкенд.** Роут модерации: режим `view=board|list` (дефолт board).
      board группирует покупки по статусам в 3 ведра: «На проверке»
      (pending_ocr+manual_review), «Подтверждено» (approved), «Отклонено»
      (rejected+revoked). Выпадающий список мероприятий с дефолтом «Все мероприятия»
      (event_id=None). Флаг `needs_attention` (auto_confirm включён, но ушло в ручную
      из-за abuse/невалидности) → красный «!» в карточке колонки «На проверке».
      Пагинация/лимит на колонку (напр. 50 + «ещё»). Кнопка «Списком» сохраняет выбор
      мероприятия.
- [ ] **8.9.2 Фронт.** `moderation_board.html` — 3 колонки (канбан), карточки с
      ключевой инфо + красный флаг, ссылка в карточку покупки. Кнопка-переключатель
      board/list. Аккуратно, обе темы, сканируемо. КП: канбан рендерится, фильтр и
      «Списком» работают, флаг виден. **Скилы:** design-taste-frontend,
      frontend-design-direction, agent-browser. **Агент:** coder (sonnet) для бэка;
      вёрстку канбана — architect (opus) или coder со скилами дизайна.

### P5 — Участники (тяжёлая переделка)

#### 8.10 Вкладка «Участники»: «Все мероприятия» + карточка (edit.md #9)
- [ ] **8.10.1 Список.** Выпадающий список + пункт «Все мероприятия» (дефолт).
      Столбцы: ВК id, Имя ВК, ФИО, Телефон, в каких мероприятиях участвует (включая
      остановленные). При «Все» — агрегировать участника по vk_user_id (один человек =
      одна строка, список его мероприятий). **Оптимизация:** серверная пагинация
      (LIMIT/OFFSET, напр. 50/стр) + индексы; НЕ грузить всё разом. При выборе
      конкретного мероприятия — как сейчас (с номерами), но строки кликабельны → карточка.
- [ ] **8.10.2 Карточка участника** `/participants/{...}`: все данные (ВК id/имя/
      ссылка, ФИО, телефон, публичное имя); все его чеки со структурой по мероприятиям
      + статус + ссылка на страницу чека (модерация); все билеты по мероприятиям
      (кол-во + номера). При «Все мероприятия» ключ карточки = vk_user_id (агрегат по
      событиям); при конкретном — participant.id. КП: список с пагинацией не лагает,
      карточка показывает чеки+билеты по мероприятиям, переходы работают.
      **Скилы:** fastapi-patterns, design-taste-frontend, agent-browser. **Агент:**
      coder (sonnet) бэк; вёрстка — со скилами дизайна.

#### 8.11 Поиск во вкладке «Участники» (edit.md #10, #18)
- [ ] **8.11.1** Поиск по выбранному scope: по имени ВК, ФИО, телефону, vk id —
      всегда. По номеру билета — ТОЛЬКО при выбранном конкретном мероприятии (при
      «Все» — поиск по номеру отключён). КП: поиск работает в обоих режимах согласно
      правилам. **Скилы:** fastapi-patterns, agent-browser. **Агент:** coder (sonnet).

### 8.12 Финальный деплой + приёмка
- [ ] **8.12.1** Полный `pytest` зелёный, alembic head, деплой всех групп на боевой,
      сквозная smoke + agent-browser проверка ключевых экранов в обеих темах, передача
      заказчику для ручного прогона бот-сценария в ВК.

---

## Журнал решений

- 2026-06-25 — **Стек Python** (а не Node): решающий фактор — OCR чеков на русском
  (Tesseract/pytesseract — родная экосистема), зрелые ВК-либы, быстрая сборка
  админки на FastAPI+Jinja2.
- 2026-06-25 — **PostgreSQL** как единственный источник истины (по требованию
  разработчика); Google Sheets — только публичное зеркало.
- 2026-06-25 — **Проверка оплаты без банка**: статичный QR не позволяет надёжно
  сматчить платёж с человеком. Делаем OCR-распознавание суммы/реквизитов + тумблер
  автоподтверждения + ручную модерацию в админке (статусы меняются в обе стороны).
- 2026-06-25 — **Кол-во постеров = floor(сумма ÷ цена)**; несоответствия → ручная
  очередь.
- 2026-06-25 — **Google-таблица создаётся автоматически** при создании мероприятия
  и привязывается к нему.
- 2026-06-25 — **OCR — Tesseract локально** (без облака): бесплатно, перс. данные
  не уходят наружу; цена — ниже точность на плохих фото (больше уйдёт в ручную
  очередь, что приемлемо).
- 2026-06-25 — **Админка по IP** (заказчику без домена), демо у разработчика — по
  своему домену; HTTPS опционально.
- 2026-06-25 — **Скрины из TZ — это конкурент**: копируем механику, не данные.
  «СИГМА» не используется; получатель платежа у заказчика свой, настраивается.
- 2026-06-25 — **Reverse proxy: Caddy вместо nginx** (по требованию заказчика). На
  боевом сервере уже работает Caddy с кучей других сайтов — чтобы их не задеть,
  админка подключается отдельным фрагментом (`caddy/vk_admin.caddy`) через
  `import /etc/caddy/conf.d/*.caddy` на отдельном порту 8080. nginx-конфиг удалён.
- 2026-06-25 — **Автодеплой-скрипт удалён** (`scripts/deploy.sh`). Заказчик хочет
  деплоить руками — в README раздел «Обновление на сервере» переписан в копипаст-
  инструкцию по шагам (git pull → pip → alembic → reload caddy → restart services).
- 2026-06-25 — **Всё параметризуется в админке** на каждое мероприятие: цена билета,
  кол-во билетов, **кол-во победителей**, **сроки работы**. Ничего не захардкожено.
- 2026-06-25 — **Один админ из `.env`** (логин открыто, пароль bcrypt-хэш); таблицы
  admins нет.
- 2026-06-25 — **Удаление мероприятия — каскадное**, чистит только его данные
  (ON DELETE CASCADE по event_id) + Google-лист; остальные не трогаются. Остановка =
  бот не реагирует.
- 2026-06-25 — **Собираем только имя** (без ФИО) + телефон. Оплата без имени/телефона
  НЕ блокирует присвоение номеров — выдаём + напоминаем участнику. Вне сценария бот
  молчит.
- 2026-06-25 — **Отзыв номеров через админку** (статус `revoked`): для непрошедшей
  оплаты/поддельных чеков — номера освобождаются и снова доступны, уходят из таблицы.
- 2026-06-25 — **Все тексты бота настраиваются на мероприятие**, не захардкожены:
  при создании предзаполняются дефолтными шаблонами, потом редактируются/заменяются.
- 2026-06-25 — **Фаза 1 завершена**: архитектура и структура репозитория — в
  `ARCHITECTURE.md`; воркер = polling 5 сек; номера — `generate_series ORDER BY
  random()`; плейсхолдеры — безопасный рендер (не сырой `str.format`).
- 2026-06-25 — **Этап 2.1 (каркас) принят**: структура пакетов, requirements,
  config (pydantic-settings), db (async engine/Base/get_session), Alembic async
  (env.py берёт URL из settings, target_metadata=Base.metadata).
- 2026-06-25 — **`participants` — per-event** (развилка §6): добавлено `event_id`
  (FK ON DELETE CASCADE), unique `(event_id, vk_user_id)`. ТЗ давало эту
  уникальность и требование «удаление чистит только данные события»; один человек
  в N розыгрышах = N строк, имя/телефон/номера изолированы по событию.
- 2026-06-25 — **`assign_unique` на Python** (а не PG `generate_series ORDER BY
  random()` из ARCHITECTURE §4.1): загрузить занятые → `random.sample` по свободным.
  Причина: диапазоны билетов малы (сотни/тысячи), вариант переносим (PG/SQLite) и
  тестируется без живого Postgres; уникальный индекс по-прежнему страхует от гонок.
  ARCHITECTURE §4.1 обновлён.
- 2026-06-25 — **Победители — display-only** (этап 2.11): таблицы winners в схеме §6
  нет, генератор показывает результат розыгрыша без персиста. Повторный розыгрыш = новый
  случайный выбор. Соответствует ТЗ.
- 2026-06-25 — **Пин `bcrypt>=4.0,<4.1`**: passlib 1.7.4 несовместим с bcrypt>=4.1.
  Не снимать верхнюю границу, пока passlib не заменён.
- 2026-06-25 — **ФАЗА 2 ЗАВЕРШЕНА**: все этапы 2.1–2.13 приняты. 105 тестов зелёных,
  1 skip (реальный OCR без бинаря Tesseract). Серверная часть (бот+воркер) и веб-
  админка (auth, мероприятия, модерация, участники, победители) готовы; артефакты
  деплоя (systemd/Caddy/README, ручной деплой) готовы. Остаётся боевое развёртывание
  заказчиком (VK-токен, Google SA-json, сервер по IP) + ручной прогон сценария в ВК.
- 2026-06-26 — **Google Sheets без OAuth**: владение выбрано «service account создаёт
  + авто-шар заказчику как редактору» (оригинал в Drive заказчика, не копия; владелец
  формально — SA). OAuth-кнопку «Подключить Google» отклонили: scope `spreadsheets`
  «чувствительный» → нужна верификация приложения Google (недели) + протухание токена
  в test-режиме. Цена/удобство в пользу авто-шара.
- 2026-06-26 — **Настройки заказчика в БД, не в `.env`**: VK-токен/group_id/почта —
  таблица `app_settings`, редактируются в админке `/settings` с кнопками проверки
  подключения. Токен шифруется (Fernet, `SECRETS_KEY`). Разовые шаги разработчика при
  деплое сведены к двум: создать Google SA-json и сгенерить `SECRETS_KEY`. **ФАЗА 3
  ЗАВЕРШЕНА**: 118 passed, 1 skipped.
- 2026-06-26 — **ФАЗА 5 ЗАВЕРШЕНА**: 152 passed на боевом сервере, оба сервиса active.
  Все пункты edit.md выполнены: тёмная тема, редизайн, Google Sheets (2-й режим),
  E2E тесты. Заказчик тестирует оба режима (HTML-таблица vs Google Sheets).
- 2026-06-26 — **РЕВИЗИЯ edit.md выявила недоделки → ФАЗА 6.** Найдено: (1) VK-ссылка
  на чат сломана (event_id вместо group_id + кривой URL); (2) в режиме Google Sheets
  участникам НЕ уходит ссылка на гугл-таблицу (всегда /p/{id}); (3) сообщение «просьба
  контактов» не убрано (edit.md просил убрать). Плюс заказчик требует ПОЛНОЙ переделки
  фронтенда. Решения: чиним 1–3, фронт перестраиваем как токен-систему (скилы
  frontend-design-direction + design-taste-frontend).
- 2026-06-26 — **«Второй режим» = поле `google_sheet_url` на мероприятие**, а не
  отдельная админка (упрощение исходной формулировки edit.md «по сути отдельная
  админка»): заказчику достаточно тестировать пер-ивент, меньше кода/инфраструктуры.
- 2026-06-26 — **Права доступа к гугл-таблице**: админ даёт ОДНУ ссылку с правом
  редактирования (под service account, бот пишет данные); читателям бот шлёт
  производную `/preview`-ссылку (read-only вид). Таблица должна быть расшарена
  «по ссылке: просмотр» — ответственность админа (подсказка в форме). Отдельные
  ссылки с разными правами при создании НЕ вводим — это лишняя сложность для заказчика.
